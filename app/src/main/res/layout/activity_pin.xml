<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/pin_bg">

    <!-- PIN display -->
    <TextView
        android:id="@+id/txtPin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="• • • •"
        android:textSize="30sp"
        android:textColor="#FFFFFF"
        android:fontFamily="sans-serif-medium"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/keypad"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="40dp"/>

    <!-- Keypad -->
    <androidx.gridlayout.widget.GridLayout
        android:id="@+id/keypad"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:columnCount="3"
        android:padding="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.65"
        app:layout_constraintWidth_percent="0.9">

        <!-- Row 1 -->
        <Button style="@style/PinKeyButton" android:text="1" android:onClick="onNumberClick"/>
        <Button style="@style/PinKeyButton" android:text="2" android:onClick="onNumberClick"/>
        <Button style="@style/PinKeyButton" android:text="3" android:onClick="onNumberClick"/>

        <!-- Row 2 -->
        <Button style="@style/PinKeyButton" android:text="4" android:onClick="onNumberClick"/>
        <Button style="@style/PinKeyButton" android:text="5" android:onClick="onNumberClick"/>
        <Button style="@style/PinKeyButton" android:text="6" android:onClick="onNumberClick"/>

        <!-- Row 3 -->
        <Button style="@style/PinKeyButton" android:text="7" android:onClick="onNumberClick"/>
        <Button style="@style/PinKeyButton" android:text="8" android:onClick="onNumberClick"/>
        <Button style="@style/PinKeyButton" android:text="9" android:onClick="onNumberClick"/>

        <!-- Row 4 -->
        <Button style="@style/PinKeyButton" android:text="⌫" android:onClick="onDeleteClick"/>
        <Button style="@style/PinKeyButton" android:text="0" android:onClick="onNumberClick"/>
        <Button style="@style/PinKeyButton" android:text="✓" android:onClick="onOkClick"/>

    </androidx.gridlayout.widget.GridLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
