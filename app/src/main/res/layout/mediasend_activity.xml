<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:id="@+id/mediasend_fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <FrameLayout
        android:id="@+id/mediasend_bottom_bar"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <include
            android:id="@+id/mediasend_count_container"
            layout="@layout/view_mediasend_count" />

        <ImageView
            android:id="@+id/mediasend_camera_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginBottom="32dp"
            android:layout_marginStart="32dp"
            android:layout_gravity="bottom|start"
            android:padding="12dp"
            android:src="@drawable/ic_camera"
            app:tint="?android:textColorPrimary"
            android:background="@drawable/media_camera_button_background"
            android:elevation="4dp"
            android:visibility="gone"
            tools:visibility="visible"/>

    </FrameLayout>

</FrameLayout>